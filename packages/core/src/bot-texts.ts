/**
 * Тексты, которыми бот говорит с клиентом.
 *
 * В коде, а не в справочнике: их три, меняются они редко и вместе с
 * тоном всего сервиса — то есть тем же заходом, что и остальные
 * формулировки. Справочник «ключ — текст» с правкой из панели здесь был
 * и убран: администратор им не пользовался, а тексты, не проходящие
 * ревью, расходились с остальным приложением молча.
 */

export const botTextKeys = ['greeting', 'support', 'referral'] as const;
export type BotTextKey = (typeof botTextKeys)[number];

export const BOT_TEXTS: Record<BotTextKey, string> = {
  greeting:
    'Здравствуйте! Это обменник TOBEE. Меняем USDT и рубли в обе стороны, ' +
    'переводом или наличными, выдаём баты (на тайский счёт или по ' +
    'PromptPay) и юани (на Alipay). Лиры, доллары, евро, рупии и ранды ' +
    'пока в разработке.\n\n' +
    'Курс виден до подачи заявки, по нему и обменяем. Откройте обменник, ' +
    'выберите направление и сумму. Заявку возьмёт менеджер, а бот напишет ' +
    'вам на каждом шаге.\n\n' +
    'Там же то, что нужно за границей: иностранная карта, оплата отеля и ' +
    'покупки в зарубежном магазине.\n\n' +
    'Курс можно посмотреть прямо здесь, кнопкой ниже. Рядом с ней ссылка, ' +
    'чтобы позвать знакомых, и поддержка. Список команд открывается кнопкой ' +
    'у поля ввода.',
  support:
    'Напишите вопрос прямо сюда, в этот чат. Его прочитает менеджер и ' +
    'ответит здесь же. Держать приложение открытым для этого не нужно.',
  referral:
    'Меняю здесь USDT и рубли. Курс видно сразу, заявку ведёт менеджер. ' +
    'Заходите по ссылке:',
};

export function botText(key: BotTextKey): string {
  return BOT_TEXTS[key];
}

/**
 * Что написано в чате до нажатия «Начать».
 *
 * Экран этот сервис показывает первым и единственный раз: клиент пришёл
 * по ссылке, увидел пустоту с логотипом и одной синей кнопкой — и решает,
 * нажимать ли. Приветствие здесь не поможет, его показывают уже нажавшему.
 *
 * Поэтому текст отвечает на вопросы «куда я попал» и «что тут делают», а
 * не здоровается. Telegram даёт на него 512 знаков и рисует без разметки:
 * жирного и ссылок здесь не бывает, абзацы — единственное членение.
 *
 * Ставится при развёртывании вместе со списком команд, а не в ответе на
 * обновление: это свойство самого бота в Telegram.
 */
export const BOT_DESCRIPTION =
  'Обменник USDT, рублей и наличных. Курс виден до подачи заявки, ' +
  'по нему и обменяем.\n\n' +
  'Выдаём баты на тайский счёт и по PromptPay, юани на Alipay. Лиры, ' +
  'доллары, евро, рупии и ранды — в разработке.\n\n' +
  'Здесь же то, что не берёт российскую карту за границей: иностранная ' +
  'карта, оплата отеля, покупки в зарубежных магазинах.\n\n' +
  'Нажмите «Начать».';

/**
 * Строка о боте: её показывают в его профиле и в поиске, рядом с именем.
 *
 * Telegram даёт 120 знаков, и читают её мельком, поэтому здесь только то,
 * чем сервис отличается от соседнего: курс называется до заявки, а ведёт
 * её человек.
 */
export const BOT_SHORT_DESCRIPTION =
  'Обмен USDT, рублей и наличных. Курс виден сразу, заявку ведёт менеджер.';
