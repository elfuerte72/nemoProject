/**
 * Поток событий POS-терминала для открытых вкладок: адрес и ритм.
 *
 * Устроен как поток панели (docs/adr/0014): говорит только сервер, и
 * говорит одно слово — «этот счёт изменился». Что именно, вкладка
 * узнаёт, перечитав свой экран.
 */

export const POS_STREAM_PATH = '/api/pos/stream';

/**
 * Как часто сервер напоминает о себе в молчащий поток. Молчащее
 * соединение закрывают промежуточные узлы, и делают это молча.
 */
export const POS_HEARTBEAT_MS = 30_000;

/** Сколько живёт одно соединение: браузер откроет заново сам. */
export const POS_STREAM_MAX_MS = 30 * 60_000;
