/**
 * Команды клиентского бота — то, что клиент видит в списке рядом с полем
 * ввода.
 *
 * Отдельным файлом без единого импорта: список нужен и боту, и скрипту
 * развёртывания, а тот запускается вне сборки Next и путей вида `@/lib`
 * не понимает.
 *
 * Обработчики этих команд — в `bot.ts`, рядом с подписями кнопок меню.
 * Список и обработчики обязаны совпадать: команда, которой нет в
 * обработчиках, покажет клиенту молчание.
 */
export const BOT_COMMANDS = [
  { command: 'start', description: 'Меню обменника' },
  { command: 'rates', description: 'Курс USDT' },
  { command: 'referral', description: 'Реферальная ссылка' },
  { command: 'support', description: 'Задать вопрос менеджеру' },
];
