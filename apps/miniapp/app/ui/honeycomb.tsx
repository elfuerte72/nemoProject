/**
 * Сота — поверхность, а не иконка.
 *
 * Тем же шестиугольником, что стоит в знаке сервиса и в знаках нижней
 * панели, набран блок «За границей»: четыре ячейки сложены, как в улье,
 * со сдвигом второго ряда на полшага. Геометрия знака стала раскладкой.
 *
 * Рисуется фигура SVG, а не `clip-path` на самой кнопке: у соты знака
 * углы скруглены, а `clip-path: path()` не тянется вслед за шириной
 * экрана — координаты в нём абсолютные. Пропорцию держит контейнер
 * блока, поэтому здесь достаточно занять его целиком.
 *
 * Цвет заливки приходит переменной `--cell` от кнопки: наведение и
 * нажатие — это её состояния, и знать о них фигуре незачем.
 */

/** Сота со скруглёнными углами в сетке 132×152 — той, в которой посчитан блок. */
const HONEY_CELL =
  'M58.2 4.5 Q66 0 73.8 4.5 L124.2 33.5 Q132 38 132 47 L132 105 Q132 114 124.2 118.5 L73.8 147.5 Q66 152 58.2 147.5 L7.8 118.5 Q0 114 0 105 L0 47 Q0 38 7.8 33.5Z';

/**
 * Кольцо фокуса нарисовано внутри фигуры, а не задано `outline`:
 * кнопка обрезана по форме соты, и обводку вокруг неё срезало бы вместе
 * с углами — работающий с клавиатуры не видел бы, где стоит.
 */
export function HoneyCellShape() {
  return (
    <svg className="honey__shape" viewBox="0 0 132 152" fill="none" aria-hidden="true">
      <path className="honey__face" d={HONEY_CELL} />
      <path className="honey__ring" d={HONEY_CELL} />
    </svg>
  );
}
