/**
 * Ожидание внутри раздела — тем же талисманом, что и на заставке.
 *
 * Строку «загружаем…» она заменяет не ради красоты: словами это
 * сообщают тогда, когда сказать больше нечего, а у сервиса есть кому
 * подождать вместо клиента. Тот же талисман на входе и внутри — это
 * ещё и одно и то же обещание: занято, но живо.
 *
 * Талисмана в этот момент везти уже не нужно — заставка показала его
 * первой, и картинка лежит в кэше. Оттого здесь и нет возни с
 * готовностью изображения: появление отдано целиком разметке.
 *
 * Появляется с задержкой. Разделы отвечают за доли секунды, и мигнувший
 * на них талисман читался бы сбоем отрисовки — а с задержкой быстрая
 * загрузка проходит вовсе без него, и это правильный исход.
 */
export function Loading() {
  return (
    <div className="loading" role="status">
      <span className="loading__halo" />
      <img
        className="loading__mascot"
        src="/tobee-mascot.webp"
        width={660}
        height={660}
        alt=""
        decoding="async"
      />
      {/* Диктору нужно слово: талисман для него — пустая картинка. */}
      <span className="sr-only">Загружаем…</span>
    </div>
  );
}
