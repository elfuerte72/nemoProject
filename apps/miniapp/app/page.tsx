/**
 * Каркас клиентского приложения.
 *
 * Экран обмена с двумя полями («что отдаю» / «что получаю») строится
 * после ответа на блокер C1 — до него неизвестен список валютных
 * направлений, а он определяет и форму экрана, и справочник в базе.
 */
export default function HomePage() {
  return (
    <main
      style={{
        fontFamily: 'system-ui, sans-serif',
        padding: '2rem 1.25rem',
        maxWidth: 480,
        margin: '0 auto',
      }}
    >
      <h1 style={{ fontSize: '1.25rem', marginBottom: '0.75rem' }}>Обмен валют</h1>
      <p style={{ lineHeight: 1.5, opacity: 0.75 }}>
        Приложение подключено. Экран обмена появится после согласования списка
        валютных направлений.
      </p>
    </main>
  );
}
