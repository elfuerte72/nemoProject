# Операции кабинета клиента: счёт, сводка за период, рефералы, QR

Status: done
Этап: 2
Blocked by: 01

`getBonusAccount` расширяется до вида кабинета (спека, «Операции
ядра»): `lines[]` по глубине с `count`, `rateBps`, `source`, `tierName`;
`tier` — текущий, следующий, активных рефералов, сколько до
следующего; `codes` — действующие с видом и названием; `canEnterPromo`.
`referralCode` в виде остаётся основным кодом — его шлёт бот и лист
«Пригласить»; `codes` рядом.

`referral-cabinet-stats.ts`: `summarizeReferralCabinet(actor, period,
{offsetMinutes})` по образцу `summarizeMerchant` и теми же помощниками
`analytics.ts`: текущий и прошлый период — пришли по линиям, стали
активными (первая исполненная заявка в периоде), начислено, выплачено;
`pending` без периода; оборот приведённых по валютам отданной стороны
исполненных в периоде заявок рефералов до глубины; дни за две недели
(пришли, начислено); по кодам — пришли и начислено с первой линии.
Один пакет запросов, `cache` не нужен — Mini App зовёт маршрутом.

`listMyReferrals(actor, {line?, offset?, limit?})` — обезличенные
строки по спеке, смещением (курсор нёс бы идентификатор реферала),
предел 50, порядок устойчив на одной дате вторым ключом. Имён и идентификаторов рефералов в ответе нет — закрепить тестом на
форму ответа.

Маршруты Mini App: `GET /api/bonus-account` (расширенный вид),
`GET /api/referral-stats?from&to&offset`, `GET /api/referrals?line&offset&limit`,
`GET /api/referral-codes`, `POST /api/referral-codes`,
`POST /api/referral-codes/[id]/archive`, `POST /api/promo` (ввести чужой
промокод), `GET /api/referral-qr/[code]` — публичный, только для
действующего кода, PNG через `qrcode` (зависимость Mini App серверная),
`cache-control` на сутки; неизвестный код — 404.

Тесты ядра: сводка на сиде с известными числами (пришли/активные/
начислено/выплачено и сравнение с прошлым периодом), оборот в двух
валютах не складывается, реферал третьей линии при глубине два в
сводку не попадает, дни по часам Бангкока, курсор списка на строках с
одной датой, чужой клиент в списке не виден.

## Comments

**8 сентября 2026, сделано.** Ядро: `getBonusAccount` отдаёт `codes` и
`canEnterPromo` (то же правило, что у `bindReferrerByPromoCode`);
`referral-cabinet-stats.ts` — `summarizeReferralCabinet` одним пакетом
запросов: пришли по линиям, стали активными (первая исполненная заявка
по подзапросу `min(completed_at)`), начислено и выплачено, оборот
приведённых по валютам отданной стороны через `referrals` (пара одна,
заявка считается раз), дни за две недели, по кодам — пришли и
начислено первой линией; `pending` без периода; период не длиннее
года. `listMyReferrals` — обезличенные строки, смещением, а не
курсором: курсор нёс бы идентификатор реферала (спека поправлена).
`lookupReferralCode` — публичный поиск кода для QR. `localMidnight`
переехал в `analytics.ts`, сводка мерчанта берёт его оттуда.

Маршруты Mini App: `GET /api/referral-stats?from&to&offset`,
`GET /api/referrals?line&offset&limit`, `GET|POST /api/referral-codes`,
`POST /api/referral-codes/[id]/archive`, `POST /api/promo` (уведомление
рефереру тем же доставщиком, что при регистрации),
`GET /api/referral-qr/[code]` — PNG через `qrcode` на сервере,
зависимость Mini App серверная, тихая зона четыре модуля, сутки кэша,
404 на незнакомый или архивный код. Адрес без `.png`: сегмент Next не
несёт расширения, тип говорит заголовок.

Тесты: `referral-cabinet-stats.test.ts` — числа периода и прошлого
периода на сиде (второй-четвёртый клиенты по датам), оборот в двух
валютах не складывается, третья линия при глубине два не видна, дни по
Бангкоку уезжают на сутки, ждёт выплаты, по кодам, только клиенту и не
длиннее года; список — обезличенность по сериализации, линия,
смещение, одинаковые даты без потерь и повторов; чужая сеть не видна;
выплачено по дате списания; счёт — коды и `canEnterPromo` до и после
заявки. Даты в сыром `sql` — строкой ISO с приведением: колонка
подзапроса маппинга не несёт.

По ревью спеки: «пришли» по кодам считаются датой привязки, как и по
линиям (спека дополнена), `via` в списке — только свой код условием
соединения; `referralCode` в счёте остаётся основным кодом сознательно.
`CHANGELOG` — с экранами тикетов 04–05: снаружи этот этап ещё не виден.
Подписи на экране (тикет 04) должны различать «стали активными за
период» (все линии) и «активных рефералов» уровня (первая линия).
