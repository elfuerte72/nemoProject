# Документы и выкатка

Status: ready-for-agent
Этап: 6, идёт с каждым этапом
Blocked by: 01

ADR 0019 «Реферальная программа настраивается: глубина, уровни, личные
ставки, несколько кодов» — решения из спеки: цепочка до пятой линии,
старшинство ставок, уровень по активным рефералам первой линии,
промокод после регистрации только без заявок, коды таблицей с
expand/contract колонки. `CONTEXT.md` — термины из спеки. `CLAUDE.md` —
абзац о реферальном кабинете. `CHANGELOG.md` тем же коммитом на каждый
этап (клиенту — кабинет, администратору — подраздел и карточка).
`backlog.md`: оплата баллами части заявки, сводка раз в неделю, порог
уровня по обороту, `/promo` в боте, удаление `clients.referral_code`
следующей миграцией после выката, уровень в строке начисления.

Переменных окружения не прибавляется; `qrcode` — зависимость Mini App,
серверная. Выкатка на dev по push после слияния PR: миграция меняет
`clients` (nullable колонка и новая) — порядок из памяти: снять
автодеплой Mini App, слить, панель накатит, Mini App руками. Смоук на
dev: `GET /api/health`, кабинет в @devnemobot_bot глазами, регистрация
второго тестового аккаунта по промокоду. `pnpm release` — по слову
Пенкина.

Приёмка по скиллу `qa` перед PR.
