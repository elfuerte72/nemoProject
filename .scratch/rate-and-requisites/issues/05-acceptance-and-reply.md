# Пачка 5. Приёмка и ответ владельцу

Status: ready-for-human

Спека: `../spec.md`, «Заметки → Ответ владельцу». История 32.

## Что сделать

- Полный прогон: `pnpm typecheck`, `pnpm test`, сборка обоих приложений.
- Финальный `/code-review` ветки против `origin/dev`.
- Ответ владельцу — `reply-to-owner.md` рядом со спекой: что сделано;
  почему на покупке 87,25, а не 87,21 (наценка берётся с той суммы,
  которую клиент отдаёт: `85,5 / 0,98`, а не `× 1,02`; переход на
  `× 1,02` — отдельная правка ядра и расчёта дохода, если настоит);
  вопросы: QR из банка и из кошелька идут по разным ценам — верно ли;
  бывает ли Alipay по e-mail; просьба прислать оригиналы обеих картинок
  с QR — фикстурой и для сверки формата.
- PR в `dev` с описанием по пачкам.

## Comments

28 августа 2026, сделано агентом; осталось человеку — см. ниже.

- Прогон: `pnpm typecheck` 10/10; `pnpm test` 10/10 — types 115, db 32,
  core 640, miniapp 75, admin 54, rates 81, crypto 29, http 4,
  concierge 20; сборка обоих приложений (`/` Mini App — 156 kB, `jsqr`
  отдельным чанком).
- Финальное ревью ветки по двум осям (стандарты, спека) против
  `origin/dev`: находки закрыты — две правды в `CLAUDE.md` (роды записи,
  «прочие пять только переводом»), комментарий у `payout_method`,
  разорванный doc-комментарий в карточке панели, дробный рубль в тесте
  сообщения бота, допущения про сетку у QR и e-mail у Alipay записаны в
  `backlog.md` с причиной.
- Ответ владельцу — `reply-to-owner.md`; отправляет Пенкин после
  выката на dev и проверки глазами в Telegram (форма, галерея на iOS и
  Android, «Сохранить картинкой» на iPhone). Перед отправкой сверить,
  что на проде включена сетка CNY / кошелёк.
- Тексты статей сида проверены правилом машинного набора вручную
  (13 статей, замечаний нет); теста на них нет — сид с базой.
