# Поднять отложенную ветку реферальной программы на свежий dev

Status: ready-for-agent
Спека: `../spec.md`

Ветка `feature/referral-cabinet` отошла от `dev` 7 сентября 2026
(PR #111) и с тех пор отстала на восемнадцать коммитов: сетки бата,
файлы оператора, ярлыки банков, документация.

Что сделать:

- влить свежий `dev` в ветку (не перебазировать: коммиты уже
  показывались и на них ссылается спека);
- отбросить `bb673e8` — WIP кабинета в Mini App, тикеты 04–05 той
  спеки. Оставить `570e1b5` (спека), `07829b0` (ядро программы),
  `f788e90` (сводка, коды, API Mini App), `6933f61` (панель);
- разобрать конфликты в десяти файлах: `CHANGELOG.md`, `CONTEXT.md`,
  `backlog.md`, `packages/core/src/index.ts`,
  `packages/types/src/index.ts`, `packages/core/src/clients.ts`,
  `packages/core/src/notifications.ts`,
  `packages/core/src/webhooks.test.ts`, `apps/admin/app/globals.css`,
  `apps/miniapp/app/globals.css`;
- проверить, что миграция `0031_referral_program` встаёт на базу,
  где последняя — `0030_webhooks`;
- прогнать тесты всех пакетов: `run test`.

Проверка: Профиль Mini App показывает то же, что показывал до работы, —
баланс, «заработано всего», две линии, ссылка, вывод. Экран не должен
измениться по виду, хотя данные под ним теперь приходят списком линий.
