# Документы, окружения, домены и выкатка

Status: ready-for-human
Этап: 7, идёт с каждым этапом

Домены — решение от 6 сентября 2026: пока идёт разработка, кабинет
живёт на бесплатном домене Dokploy (`sslip.io`, как miniapp и admin),
сначала только на `dev`; письма там в режиме `EMAIL_DELIVERY=log`
(тикет 04), ссылки читаются из журнала контейнера. На боевой контур
кабинет выкатывается только после покупки домена: без своего DNS
провайдер писем не отправит, а адрес `sslip.io` с IP сервера уходит в
код мерчантов и в письма. На аккаунте Hostinger доменов нет (проверено
6 сентября 2026), зона `mxpkn8ns.ru` уехала на чужой IP.

От владельца до боевого выката: домен (например `business.<домен>`
для кабинета и API, `sandbox.<домен>` для песочницы, отправители
`noreply@<домен>` и `noreply@sandbox.<домен>`), доступ к DNS для DKIM,
SPF, DMARC и записей доменов, ник Telegram для поддержки мерчантов,
выбор почтового провайдера и оплата его тарифа. Список DNS-записей под
выбранного провайдера составить здесь, когда домен куплен.

Dokploy, оба окружения: третье приложение `cabinet` из той же ветки,
переменные (`DATABASE_URL`, `REQUISITES_PUBLIC_KEY`, `CABINET_URL`,
`CABINET_SESSION_SECRET`, `EMAIL_API_KEY`, `EMAIL_FROM`,
`API_KEY_PREFIX`, `SCHEDULER_SECRET`, `NEXT_PUBLIC_*` через
`buildSecrets` — память: `saveEnvironment` требует их), домен,
расписание чистки журнала API. Управление по SSH через `/root/dk`
(память: панель снаружи мертва). Проверка выката —
`GET /api/health` у кабинета, как у остальных.

Документы, каждым этапом тем же коммитом: `CHANGELOG.md` (видимое
мерчанту и менеджеру); README — третье приложение, переменные, чек-лист
выкатки (домены, DKIM, расписание), «Два контура» с песочницей;
`CLAUDE.md` — `apps/cabinet` и `packages/ui`, `packages/email` в списке,
абзац о мерчанте как втором владельце и о том, чего у него нет;
`CONTEXT.md` — термины «Мерчант», «Получатель», «Ключ API», «Вебхук»,
«Песочница»; ADR из тикетов 01 и 07; `backlog.md` — записи, сделанные 6
сентября 2026 при планировании (аутентификатор мерчанту, тестовые
ключи на проде, английский, переписка, несколько людей у мерчанта,
отдельная услуга).

Приёмка по скиллу `qa` перед каждым PR и живой прогон на песочнице по
критериям спеки перед `pnpm release` этапов 1, 3 и 4.

## Уточнено 7 сентября 2026

Денег почтовый провайдер не требует: у Resend бесплатный тариф — 3 000
писем в месяц и 100 в сутки, и этого хватает с запасом (кабинету письмо
уходит на регистрацию, сброс пароля, решение по анкете и переходы
заявок). Упирается всё в домен: отправлять с собственного адреса на
чужие ящики можно только с домена, подтверждённого записями в DNS. До
покупки домена почта работает режимом `EMAIL_DELIVERY=log` — письмо
целиком пишется в журнал, ссылка забирается оттуда; это и есть «mock»,
о котором договорились.

## Comments

**7 сентября 2026, документы.** Из перечисленного в тикете сделано с
тикетами 06–09: `CHANGELOG.md` тем же коммитом на каждый этап; README —
третье приложение, переменные, `packages/ui`, `packages/email`,
`packages/qr`, «Два контура» со строкой про песочницу и `GET /api/health`
у трёх приложений; `CLAUDE.md` — `apps/cabinet` и пакеты в списке,
абзацы о мерчанте, API, вебхуках, форме заявки и сводке; `CONTEXT.md` —
«Мерчант», «Ключ API», «Вебхук», «Получатель», «Песочница»; ADR 0017 и
0018; записи в `backlog.md`. PR в `dev` — #110.

За человеком остаётся всё, что требует домена и доступа: третье
приложение `cabinet` в Dokploy на обоих контурах с переменными из
README, расписание `POST /api/maintenance/purge-logs`, ник поддержки в
настройках панели dev и прода, домены, DKIM/SPF/DMARC, отправитель
писем; после выката — `GET /api/health` кабинета и живой прогон по
критериям спеки на песочнице.

**7 сентября 2026, образ и dev.** Образа, из которого Dokploy собрал бы
кабинет, не было вовсе — заведён `docker/cabinet.Dockerfile` по образцу
клиентского: контекст в корне, без приватного ключа (ADR 0002), без
миграций, порт 3000 в контейнере. README «Развёртывание» переписан на
три приложения: шаг с переменными кабинета, ник поддержки мерчантов в
шаге о настройках, раздел «Почта мерчантам: домен и DNS» — записи
Resend (SPF на `send`, DKIM в форме, которую показывает Resend, DMARC),
адреса `noreply@<домен>` и `noreply@sandbox.<домен>` из этого тикета.
Список DNS составлен до покупки домена: провайдер выбран, а значения
записей свои у каждого домена и берутся у Resend.

На Dokploy в окружении `dev` заведено третье приложение `cabinet`
(`rbcFm3GM0b7gEkMzETbdG`, appName `app-input-open-source-array-i77zih`
— имя сгенерировал Dokploy, переименование через `application.update`
он не принял), ветка `dev`, `docker/cabinet.Dockerfile`, автодеплой по
push; домен
`https://app-input-open-source-array-i77zih-0d6d6a-177-7-34-106.sslip.io`;
переменные: `DATABASE_URL`, `REQUISITES_PUBLIC_KEY`, `ADMIN_URL`,
`SCHEDULER_SECRET` те же, что у dev miniapp, `CABINET_URL` на свой
домен, свой `CABINET_SESSION_SECRET`, `API_KEY_PREFIX=sk_test_`,
`EMAIL_DELIVERY=log`; расписание «Журнал вызовов API: убрать старше
тридцати дней» в 04:20 по часам сервера. Соберётся оно первым push в
`dev` — слиянием PR #110. Боевой контур по-прежнему ждёт домена.

За человеком остаётся: домен, DNS по README, отправитель, ник поддержки
в настройках панели (dev и прод), кабинет на `production` после покупки
домена.

**7 сентября 2026, dev выкачен, прогон прошёл.** PR #110 слит в `dev`
(340634f) по порядку из памяти: автодеплой Mini App снят → слияние →
панель накатила миграции 0028–0030 (`migrations applied successfully`)
→ кабинет собрался и поднялся (`GET /api/health`: версия 340634f, база
ok, почта `log`) → Mini App выкачен руками, автодеплой возвращён.
Живой прогон на песочнице — та часть критериев спеки, что доступна из
кабинета и API: регистрация
`oplatishka@example.com` через кабинет, ссылка подтверждения из
журнала контейнера, подтверждение, одобрение одноразовым скриптом из
контейнера панели (`core.approveMerchant` от 379336096), вход;
`probe-api.mts` — ключ, курсы (16 пар), котировка обеих сторон, запись,
заявка с получателем в теле, повтор `Idempotency-Key` вернул ту же
заявку, карточка, отмена, чужой ключ 401, подпись, отзыв, журнал
вызовов; `probe-webhooks.mts` — точка, пробная доставка с ответом
приёмника (405 от example.com), заявка и отмена по API, пауза, удаление;
письма в журнале: подтверждение, ключ выпущен, заявка отменена, ключ
отозван.

Не проверено на прогоне и остаётся за живой проверкой из панели:
одобрение кнопкой из тестовой панели (скрипт зовёт `approveMerchant`
мимо маршрута, и письмо «анкета одобрена» не порождалось); переходы
менеджера — курс подтверждён, оплата получена, исполнена — с письмом и
вебхуком на каждом; заявка мерчанта в столе менеджера с пометкой;
отключённый мерчант — 401 на ключ в ту же секунду при доходящей до
конца открытой заявке. Всё это — экраны панели, и смотреть их надо
глазами через вход по Telegram.

Три пробела по следам прогона. Переменные почты стояли только у
кабинета, а пишут мерчанту три процесса: панель (решение по анкете,
переходы менеджера) и планировщик Mini App (срок оплаты) — на dev обоим
добавлены `EMAIL_DELIVERY=log` и `CABINET_URL`, README шаги 3–5;
выключенная почта не молчит, а пишет в журнал «Письмо не отправлено».
И каждый заход на страницу кабинета без сессии писал в журнал
«⨯ Error: Нет сессии» — каркас и раздел читают сессию параллельно;
починено в `lib/session.ts` (`viewerOrElse`) с тестом, и правило
«страницы читают сессию только через `viewer()`» закреплено тестом по
файлам. Всё — в PR вслед за #110.
