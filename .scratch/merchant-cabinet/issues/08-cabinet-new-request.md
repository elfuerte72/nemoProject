# Заявка руками из кабинета, получатели, курсы

Status: done
Этап: 5
Blocked by: 05

Раздел **Новая заявка** (страница, не модал, как у Love&Pay): два поля
«отдаю / получаю» с живым пересчётом любой стороны (`Money`,
`divideCeil` для обратного счёта), выбор направления из условий
обмена, курс на черте по `lib/rate-line.ts` — вынести из Mini App в
`@nemo/types` или общий пакет с тестом, чтобы правило было одно;
`reference` и комментарий; получатель: из сохранённых или новый прямо
здесь; сводка перед подачей снимком чисел с `quotedAt`; подтверждение.
Только безналичные направления; «в разработке» показываются, но не
подаются, как в Mini App.

Раздел **Получатели**: список сохранённых реквизитов по типам из
`requisiteKindsFor`, добавление формой с проверкой правдоподобия теми
же `looksLike*` и словами `REQUISITE_COMPLAINTS`, чтение QR на
устройстве (`lib/qr.ts` из Mini App — в общий пакет с фикстурами),
архив с подтверждением. Реквизиты, заведённые из тела API, в списке не
показываются (архивированы при подаче).

Раздел **Курсы**: весь справочник направлений живым курсом, сторона
одна на столбец, как в `rates-message.ts`; минимум и срок оплаты;
подсказка о том, что курс заявки фиксируется при подаче на срок
оплаты (ADR-0006).

Тесты: перенос `rate-line` и `qr` без изменения ожиданий; обратный
счёт вверх; вёрстка глазами. `CHANGELOG.md`.

## Comments

**7 сентября 2026, сделано.** Разделы «Новая заявка» (`/requests/new`),
«Получатели» (`/recipients`) и «Курсы» (`/rates`) в `apps/cabinet`;
маршруты `GET /api/quote`, `POST /api/requests`, `GET|POST
/api/requisites`, `POST /api/requisites/:id/remove`. Проверено глазами
через Playwright с кукой из `/api/auth/login` на 1280 и 420 пикселях:
пересчёт обеих сторон, сводка, подача с новым получателем и отметкой
«запомнить» (заявка открылась карточкой, запись встала в список),
чтение PromptPay-QR из фикстуры в браузере, горизонтальной прокрутки
нет.

Переносы: `rate-line` из Mini App — в `@nemo/types` (`quote.ts`,
`rateLine`) без изменения ожиданий теста; `qr.ts` с фикстурами — в
новый пакет `@nemo/qr`. Сверх тикета, чтобы не заводить третьих копий:
туда же в `@nemo/types` уехали арифметика сторон (`payoutOf`,
`giveFor`, до этого копиями в экране Mini App и в `lib/v1/quote.ts`
кабинета), справочник валют (`currencies.ts`), подписи способов
получения (`REQUISITE_KIND_LABELS` формы) и подпись записи
(`describeRequisites`, до этого копиями в ядре и Mini App с тестом на
совпадение). `QuoteView` ядра теперь наследует `Quote` из типов.
Раздел «Курсы» и `GET /api/v1/rates` читают один список
(`lib/direction-rates.ts`). В `@nemo/ui` — `currentSection`: среди
вложенных пунктов меню текущий один, самый длинный.

Не сделано: **комментарий** к заявке — в модели заявки поля нет, и
комментарий менеджеру без переписки был бы обращением без обратного
канала; записано в `backlog.md`. Колонка «Минимум направления» на
локальной базе пуста — сеток нет; на dev и проде она заполнится из
настроек.
