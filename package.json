{
  "name": "nemo",
  "private": true,
  "version": "0.0.1",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint",
    "test": "turbo test",
    "db:generate": "pnpm --filter @nemo/db generate",
    "db:migrate": "pnpm --filter @nemo/db migrate",
    "db:studio": "pnpm --filter @nemo/db studio",
    "generate-keys": "tsx scripts/generate-requisite-keys.mts",
    "create-first-admin": "tsx --env-file-if-exists=.env scripts/create-first-admin.mts",
    "reissue-second-factor": "tsx --env-file-if-exists=.env scripts/reissue-second-factor.mts",
    "set-telegram-webhook": "tsx --env-file-if-exists=.env scripts/set-telegram-webhook.mts",
    "seed-directories": "tsx --env-file-if-exists=.env scripts/seed-directories.mts"
  },
  "dependencies": {
    "@nemo/core": "workspace:*",
    "@nemo/crypto": "workspace:*",
    "@nemo/db": "workspace:*",
    "drizzle-orm": "^0.38.3",
    "qrcode": "^1.5.4"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/qrcode": "^1.5.5",
    "tsx": "^4.23.1",
    "turbo": "^2.9.14",
    "typescript": "^5.6.0"
  }
}
